[package]
name = "netcatty-mosh"
version = "0.1.0"
description = "Secret-safe Mosh handshake parser and bounded session state machine for Goral"
edition.workspace = true
license.workspace = true
rust-version.workspace = true

[dependencies]
serde.workspace = true
uuid = { version = "1", features = ["v4"] }
zeroize = "1"

[dev-dependencies]
serde_json = "1"
