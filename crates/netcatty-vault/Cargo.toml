[package]
name = "netcatty-vault"
version = "0.1.0"
description = "Application-neutral saved-host models and durable persistence for Goral"
edition.workspace = true
license.workspace = true
rust-version.workspace = true

[dependencies]
serde.workspace = true
serde_json = "1"
sha2 = "0.10"
uuid = { version = "1", features = ["v4"] }

[dev-dependencies]
tempfile = "3"
